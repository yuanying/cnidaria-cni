# cnidaria-cni

A Kubernetes CNI for small clusters: a bridge per node, host routing between
nodes (flannel host-gw style), IPv4 / IPv6 dual stack, NetworkPolicy, and
policies for the nodes themselves.

Work in progress.
