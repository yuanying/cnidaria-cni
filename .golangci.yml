# Kept minimal on purpose: the standard linter set plus gofmt. Add a linter here when a
# class of mistake has actually shown up, not ahead of it.
version: "2"

linters:
  default: standard

formatters:
  enable:
    - gofmt
